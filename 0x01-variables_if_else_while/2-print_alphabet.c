#include <stdio.h>

/**
 * main - This function prints the alphabets in lowercases
 *
 * Return: Always (0)
 */

int main(void)

{
	char v;

	for (v = 'a'; v <= 'z'; v++)
	putchar(v);
	putchar('\n');
	return (0);
}
