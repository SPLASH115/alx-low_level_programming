#include <stdio.h>

/**
 * main - This is the entry point of this code
 *
 * Return: Always success (0)
 */
int main(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
